export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
   zipcode: string
   number: number
   complement: string
}

export type userInfo = {
   street: string
   neighbourhood: string
   city: string
   state: string
}